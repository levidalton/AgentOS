{
  "name": "agent-os",
  "version": "0.4.1",
  "description": "Portable Agent Operating System — continuity, single source of truth, and lightweight session logging for any coding agent on any project.",
  "type": "module",
  "bin": {
    "agent-os": "./bin/agent-os.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs hooks/journal.test.mjs",
    "check": "node bin/agent-os.mjs check ."
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true
}
